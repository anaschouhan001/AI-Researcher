# --- Web framework ---
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- Config / validation ---
pydantic[email]>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# --- Auth ---
pyjwt>=2.8
passlib[bcrypt]>=1.7
bcrypt<4.1  # passlib 1.7 compatibility

# --- Orchestration / LLM ---
langgraph>=0.2
langchain>=0.2
langchain-core>=0.2
langchain-google-genai>=1.0
google-generativeai>=0.7

# --- HTTP / resilience ---
httpx>=0.27
tenacity>=8.3

# --- Data / persistence (SQLite by default; Postgres optional) ---
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
alembic>=1.13
redis>=5.0
# asyncpg>=0.29  # uncomment for Postgres via DATABASE_URL

# --- Background jobs ---
celery>=5.4

# --- RAG ---
chromadb>=0.5
sentence-transformers>=3.0
rank-bm25>=0.2.2

# --- Sources ---
wikipedia-api>=0.6
arxiv>=2.1
feedparser>=6.0

# --- Logging ---
structlog>=24.1

# --- Reports ---
markdown>=3.6
weasyprint>=62 ; sys_platform != "win32"
python-docx>=1.1
jinja2>=3.1

# --- Testing ---
pytest>=8.2
pytest-asyncio>=0.23
respx>=0.21
httpx>=0.27
